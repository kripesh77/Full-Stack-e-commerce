{
  "name": "full-stack-ecommerce",
  "version": "1.0.0",
  "description": "Full-stack e-commerce application",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm ci && npm run build",
    "start": "node backend/server.js"
  },
  "author": "Kripesh Ghimire",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
