# Frontend Environment Variables

# Development - Point to backend server
VITE_API_URL=http://localhost:5000

# Optional metadata
VITE_APP_NAME=E-Commerce App
VITE_APP_VERSION=1.0.0

# Production (Unified Deployment)
# No need to set VITE_API_URL in production
# Frontend automatically uses relative URLs when served from backend
# Backend and frontend are on the same domain